import 'package:flutter/material.dart';

class ScoreSheetContent extends StatelessWidget {
  const ScoreSheetContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
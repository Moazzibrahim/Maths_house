import 'package:flutter/material.dart';

class QuizzesContent extends StatelessWidget {
  const QuizzesContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Quizzes content'),);
  }
}